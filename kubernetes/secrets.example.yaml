# Template only — DO NOT commit real secret values.
#
# Preferred: create the Secret imperatively so values never touch git:
#
#   kubectl create secret generic lawyered-backend-secrets \
#     --from-literal=GOOGLE_API_KEY=your-google-api-key \
#     --from-literal=COURTLISTENER_API_TOKEN=your-courtlistener-token
#
# Production: use Workload Identity + Secret Manager CSI driver instead of
# Kubernetes Secrets so credentials are never stored in etcd.
apiVersion: v1
kind: Secret
metadata:
  name: lawyered-backend-secrets
type: Opaque
stringData:
  GOOGLE_API_KEY: ""
  COURTLISTENER_API_TOKEN: ""
