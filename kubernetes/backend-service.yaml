apiVersion: v1
kind: Service
metadata:
  name: lawyered-backend
  labels:
    app: lawyered-backend
spec:
  type: ClusterIP
  selector:
    app: lawyered-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
